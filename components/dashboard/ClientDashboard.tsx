"use client";

import { FormEvent, useEffect, useState } from "react";
import { Bell, ChevronDown, Download, LogOut, Send } from "lucide-react";
import { useRouter } from "next/navigation";
import { usePortalProjects } from "@/hooks/usePortalProjects";
import { PortalProject, ChangeRequest } from "@/lib/portal";

const tabs = ["Active Projects", "Quotes & Invoices", "Support Tickets"];

export default function ClientDashboard() {
  const router = useRouter();
  const { projects, updateProjects } = usePortalProjects();
  const [tab, setTab] = useState(tabs[0]);
  const [profileOpen, setProfileOpen] = useState(false);
  const project = projects[0];
  const elapsed = Math.max(0, Math.floor((Date.now() - new Date(project.startDate).getTime()) / 86400000));
  function logout() { localStorage.removeItem("visioncore-auth"); router.push("/sign-in"); }
  function addRequest(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const request: ChangeRequest = { id: `CR-${Date.now()}`, type: String(form.get("type")), message: String(form.get("message")), status: "Submitted", createdAt: new Date().toISOString().slice(0, 10) }; updateProjects(projects.map((item) => item.id === project.id ? { ...item, changeRequests: [request, ...item.changeRequests] } : item)); event.currentTarget.reset(); }
  return <div className="min-h-[80vh] bg-slate-50 text-[#0A192F]"><div className="border-b border-white/10 bg-[#0A192F] text-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><div className="flex items-center gap-3"><img src="/assets/logo-horizontal.png" alt="VisionCore Technologies" className="h-9 w-auto brightness-0 invert" /><span className="hidden border-l border-white/20 pl-3 text-xs text-white/50 sm:inline">CLIENT PORTAL</span></div><div className="relative"><button onClick={() => setProfileOpen(!profileOpen)} className="flex min-h-11 items-center gap-3 rounded-full border border-white/15 bg-white/10 px-3 text-sm"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-[#FF4D15] font-bold">{project.clientName.charAt(0)}</span><span className="hidden text-left sm:block"><b>{project.clientName}</b><small className="block text-white/50">{project.company}</small></span><ChevronDown className="h-4 w-4" /></button>{profileOpen && <div className="absolute right-0 top-14 z-20 w-52 rounded-xl bg-white p-2 text-[#0A192F] shadow-xl"><p className="px-3 py-2 text-xs text-slate-500">{project.company}</p><button onClick={logout} className="flex min-h-11 w-full items-center gap-2 rounded-lg px-3 text-sm hover:bg-slate-100"><LogOut className="h-4 w-4" />Log Out</button></div>}</div></div></div><main className="mx-auto max-w-7xl px-6 py-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-widest text-[#FF4D15]">CLIENT DASHBOARD</p><div className="mt-2 flex flex-wrap items-center gap-3"><h1 className="text-3xl font-extrabold">{project.name}</h1><span className="rounded-full bg-[#FF4D15]/10 px-3 py-1 text-xs font-semibold text-[#FF4D15]">{project.status}</span></div><p className="mt-2 text-sm text-slate-500">{project.company} · Project overview</p></div><button className="flex min-h-11 items-center gap-2 rounded-full border border-slate-200 bg-white px-4 text-sm font-semibold shadow-sm"><Bell className="h-4 w-4 text-[#FF4D15]" />Notifications</button></div><div className="mt-8 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["START DATE", project.startDate], ["TIME ELAPSED", `Active for ${elapsed} Days`], ["EST. DELIVERY", project.launchDate], ["LIVE PROGRESS", `${project.progress}%`]].map(([label, value], i) => <div key={label} className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm"><p className="text-[10px] font-bold tracking-widest text-slate-400">{label}</p><p className="mt-4 text-lg font-extrabold">{value}</p>{i === 3 && <div className="mt-3 h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-[#FF4D15]" style={{ width: `${project.progress}%` }} /></div>}</div>)}</div><div className="mt-8 flex gap-2 overflow-x-auto border-b border-slate-200">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`min-h-11 whitespace-nowrap border-b-2 px-4 text-sm font-semibold ${tab === item ? "border-[#FF4D15] text-[#FF4D15]" : "border-transparent text-slate-500"}`}>{item}</button>)}</div><div className="mt-6">{tab === tabs[0] && <ProjectPanel project={project} />}{tab === tabs[1] && <FinancePanel project={project} />}{tab === tabs[2] && <SupportPanel project={project} onSubmit={addRequest} />}</div></main></div>;
}

function ProjectPanel({ project }: { project: PortalProject }) { return <div className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><div className="rounded-2xl border border-slate-200/80 bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">What has been done</h2><div className="mt-6 space-y-5">{project.milestones.map((milestone) => <div key={milestone.id} className="flex items-start gap-3"><span className={`mt-1 flex h-5 w-5 shrink-0 items-center justify-center rounded-full text-xs ${milestone.status === "Completed" ? "bg-emerald-500 text-white" : "bg-orange-100 text-orange-600"}`}>{milestone.status === "Completed" ? "✓" : "•"}</span><div><p className="text-sm font-semibold">{milestone.title}</p><p className="mt-1 text-xs text-slate-500">{milestone.timestamp} · {milestone.status}</p></div></div>)}</div></div><div className="rounded-2xl border border-slate-200/80 bg-white p-6 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-[#FF4D15]">CURRENT PHASE</p><h2 className="mt-2 text-2xl font-bold">{project.status}</h2><div className="mt-6 flex flex-wrap gap-2">{project.techStack.map((tech) => <span key={tech} className="rounded-full bg-slate-100 px-3 py-1 text-xs text-slate-600">{tech}</span>)}</div></div></div>; }
function FinancePanel({ project }: { project: PortalProject }) { return <div className="overflow-x-auto rounded-2xl border border-slate-200/80 bg-white shadow-sm"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{["Description", "Date", "Amount", "Status", "Receipt"].map((h) => <th key={h} className="px-5 py-4">{h}</th>)}</tr></thead><tbody>{project.finances.map((entry) => <tr key={entry.id} className="border-t border-slate-100"><td className="px-5 py-5 font-semibold">{entry.description}</td><td className="px-5 py-5 text-slate-500">{entry.date}</td><td className="px-5 py-5">${entry.amount.toLocaleString()}</td><td className="px-5 py-5"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${entry.status === "Paid" ? "bg-emerald-50 text-emerald-600" : "bg-orange-50 text-orange-600"}`}>{entry.status}</span></td><td className="px-5 py-5"><a href={entry.receiptUrl ?? "#"} className="flex items-center gap-1 text-xs font-semibold text-[#FF4D15]"><Download className="h-4 w-4" />Download</a></td></tr>)}</tbody></table></div>; }
function SupportPanel({ project, onSubmit }: { project: PortalProject; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) { return <div className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><form onSubmit={onSubmit} className="rounded-2xl border border-slate-200/80 bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Submit Change Request</h2><select name="type" required className="mt-5 min-h-11 w-full rounded-lg border border-slate-200 px-3 text-sm"><option value="">Change type</option><option>Design</option><option>Functionality</option><option>Content Update</option><option>Bug/Issue</option></select><textarea name="message" required rows={5} className="mt-4 w-full rounded-lg border border-slate-200 p-4 text-sm" placeholder="Message & details" /><button className="mt-4 flex min-h-11 w-full items-center justify-center gap-2 rounded-full bg-[#FF4D15] font-semibold text-white hover:bg-[#E03E0A]">Send Request <Send className="h-4 w-4" /></button></form><div className="rounded-2xl border border-slate-200/80 bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Request Status</h2><div className="mt-5 space-y-3">{project.changeRequests.map((request) => <div key={request.id} className="rounded-xl border border-slate-100 p-4"><div className="flex justify-between gap-3"><p className="text-sm font-semibold">{request.type}</p><span className="text-xs font-semibold text-[#FF4D15]">{request.status}</span></div><p className="mt-2 text-sm text-slate-500">{request.message}</p><p className="mt-2 text-xs text-slate-400">{request.createdAt}</p></div>)}</div></div></div>; }
